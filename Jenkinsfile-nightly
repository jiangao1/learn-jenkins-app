pipeline {
    agent any

    stages {
        stage('Build Docker image') {
            steps {
                echo 'Building Docker image..'
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
        }
    }
}